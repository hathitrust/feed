name: Build

on:
    workflow_run:
        workflows: ['Run Tests']
        branches: ['main']
        types: [completed]

    workflow_dispatch:
        inputs:
            img_tag:
                description: Docker Image Tag
            ref:
                description: Revision or Branch to build
                default: main
            push_latest:
                description: Set True if the build is for the latest version
                type: boolean
                required: false
                default: false
            platforms:
                description: Platforms to build for
                type: choice
                default: linux/amd64,linux/arm64
                options:
                - linux/amd64,linux/arm64
                - linux/amd64
                - linux/arm64
            rebuild:
                description: Rebuild this image?
                type: boolean
                default: false


jobs:
    build-image:
        permissions:
            contents: read
            packages: write
        name: Build Image
        uses: hathitrust/github_actions/.github/workflows/build.yml@dc1e0d1e000e898af7ac65b5ecb1e3f7d9fd1dcc # v2.1.0
        with:
            image: ghcr.io/${{ github.repository }}
            dockerfile: Dockerfile
            img_tag: ${{ inputs.img_tag }}
            tag: ${{ inputs.ref }}
            push_latest: ${{ inputs.push_latest || false }}
            rebuild: ${{ inputs.rebuild || false }}
            build-args: 'version="commit ${{ github.sha }}"'
            platforms: ${{ inputs.platforms || 'linux/amd64,linux/arm64' }}
        secrets: inherit
